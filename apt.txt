# App-specific apt extras for mit-learn.
# Common-core packages (git curl libjpeg-dev zlib1g-dev net-tools pkg-config
# libffi-dev libxmlsec1-dev libpq-dev postgresql-client build-essential) are
# baked into mitodl/ol-python-base:3.12 and no longer listed here.

# Required by cairocffi / cairosvg
libcairo2-dev

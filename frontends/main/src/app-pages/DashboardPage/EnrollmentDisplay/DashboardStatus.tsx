import { styled } from "ol-components"

const DashboardStatus = styled.div(({ theme }) => ({
  borderRadius: "50%",
  width: "16px",
  height: "16px",
  backgroundColor: theme.custom.colors.lightGray2,
}))

export { DashboardStatus }

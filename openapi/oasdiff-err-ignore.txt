# Allowlist for intentional breaking API changes, read by `oasdiff breaking
# --err-ignore` in .github/workflows/openapi-diff.yml.
#
# Each line must contain the method + path of the changed endpoint (or the
# keyword `components` for a components-section change) plus enough of the
# breaking-change message to match it. Copy the line straight out of the
# failing `openapi-diff` job's output -- oasdiff only needs the method/path
# (or `components`) and message text to line up; order and casing don't
# matter, and anything else on the line (a date, a ticket link) is fine.
#
# Example:
#   2026-08-24 GET /api/v1/foo/ removed the success response with the status '200'

# Removing the unused UserWebsite API -- nothing consumed these endpoints and
# production held no rows. https://github.com/mitodl/mit-learn/pull/3941
2026-09-15 POST /api/v0/websites/ api path removed without deprecation
2026-09-15 DELETE /api/v0/websites/{id}/ api path removed without deprecation

# `email_optin` was always nullable at runtime -- the column is
# BooleanField(null=True) with no default, so a user who never set a
# preference has always serialized as null. The spec claimed otherwise; these
# entries cover correcting it, not a change in behavior.
# https://github.com/mitodl/hq/issues/13373
2026-09-15 GET /api/v0/profiles/{user__username}/ the response property `email_optin` became nullable for the status `200`
2026-09-15 PATCH /api/v0/profiles/{user__username}/ the response property `email_optin` became nullable for the status `200`
2026-09-15 PUT /api/v0/profiles/{user__username}/ the response property `email_optin` became nullable for the status `200`
2026-09-15 GET /api/v0/users/ the response property `items/profile/email_optin` became nullable for the status `200`
2026-09-15 POST /api/v0/users/ the response property `profile/email_optin` became nullable for the status `201`
2026-09-15 GET /api/v0/users/me/ the response property `profile/email_optin` became nullable for the status `200`
2026-09-15 GET /api/v0/users/{username}/ the response property `profile/email_optin` became nullable for the status `200`
2026-09-15 PATCH /api/v0/users/{username}/ the response property `profile/email_optin` became nullable for the status `200`
2026-09-15 PUT /api/v0/users/{username}/ the response property `profile/email_optin` became nullable for the status `200`

# Reinstating the `article` resource type. Every entry below is the same
# additive change: a new `ArticleResource` variant appears in the polymorphic
# resource union, and `article` appears in the resource_type enum. No existing
# variant, property or enum value is removed or reshaped, and no endpoint
# stops returning what it returned before -- oasdiff flags it because a client
# that switches exhaustively over the union has a case it does not know.
#
# The one such client we own, the Next.js frontend, is updated in the same
# change (the Record<ResourceTypeEnum, ...> maps and the test factory).
# https://github.com/mitodl/mit-learn/pull/3944
2026-09-17 GET /api/v0/vector_learning_resources_search/ added `#/components/schemas/ArticleResource` to the `results/items/` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/featured/ added `#/components/schemas/ArticleResource` to the `results/items/` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/featured/{id}/ added `#/components/schemas/ArticleResource` to the response body `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/ added `#/components/schemas/ArticleResource` to the `results/items/` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/summary/ added the new `article` enum value to the `results/items/resource_type` response property for the response status `200`
2026-09-17 GET /api/v1/learning_resources/{id}/ added `#/components/schemas/ArticleResource` to the response body `oneOf` list for the response status `200`
2026-09-17 PATCH /api/v1/learning_resources/{id}/learning_paths/ added `#/components/schemas/ArticleResource` to the `items/resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/{id}/similar/ added `#/components/schemas/ArticleResource` to the `items/` response property `oneOf` list for the response status `200`
2026-09-17 PATCH /api/v1/learning_resources/{id}/userlists/ added `#/components/schemas/ArticleResource` to the `items/resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/{id}/vector_similar/ added `#/components/schemas/ArticleResource` to the `items/` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/{learning_resource_id}/items/ added `#/components/schemas/ArticleResource` to the `results/items/resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources/{learning_resource_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learning_resources_search/ added `#/components/schemas/ArticleResource` to the `results/items/` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/learningpaths/{learning_resource_id}/items/ added `#/components/schemas/ArticleResource` to the `results/items/resource` response property `oneOf` list for the response status `200`
2026-09-17 POST /api/v1/learningpaths/{learning_resource_id}/items/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `201`
2026-09-17 GET /api/v1/learningpaths/{learning_resource_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 PATCH /api/v1/learningpaths/{learning_resource_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/podcasts/{learning_resource_id}/items/ added `#/components/schemas/ArticleResource` to the `results/items/resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/podcasts/{learning_resource_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/userlists/{userlist_id}/items/ added `#/components/schemas/ArticleResource` to the `results/items/resource` response property `oneOf` list for the response status `200`
2026-09-17 POST /api/v1/userlists/{userlist_id}/items/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `201`
2026-09-17 GET /api/v1/userlists/{userlist_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 PATCH /api/v1/userlists/{userlist_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/video_playlists/{learning_resource_id}/items/ added `#/components/schemas/ArticleResource` to the `results/items/resource` response property `oneOf` list for the response status `200`
2026-09-17 GET /api/v1/video_playlists/{learning_resource_id}/items/{id}/ added `#/components/schemas/ArticleResource` to the `resource` response property `oneOf` list for the response status `200`

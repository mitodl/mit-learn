"""Content feedback app setup."""

from django.apps import AppConfig


class ContentFeedbackConfig(AppConfig):
    """Configuration settings for the app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "content_feedback"
